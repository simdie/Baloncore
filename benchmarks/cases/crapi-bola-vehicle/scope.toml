# BALONCORE scope contract authorizing scans of the locally-vendored crAPI stack.
[scope]
allow_urls = ["http://127.0.0.1:8888"]
allow_hosts = ["127.0.0.1", "localhost"]
deny_hosts = []
max_depth = 3
authorized = true
notes = "Locally-run OWASP crAPI Docker Compose stack (Apache-2.0) booted by bench-crapi on 127.0.0.1:8888. Do not point this scope at any other host."
